import 'package:flutter/material.dart';

class ActionsDrawer extends StatelessWidget {
  const ActionsDrawer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Drawer();
  }
}
